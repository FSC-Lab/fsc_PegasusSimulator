"""Shared pieces of the whole-body vs geometric+L1 comparison campaign.

Deliberately separate from utils_planner/utils_controller: nothing in here is
flown by the validated demos (01..06 and their launchers are untouched by this
campaign), so a change here can never move a flight-test result.
"""
